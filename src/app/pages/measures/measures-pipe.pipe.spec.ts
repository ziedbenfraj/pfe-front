import { MeasuresPipePipe } from './measures-pipe.pipe';

describe('MeasuresPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MeasuresPipePipe();
    expect(pipe).toBeTruthy();
  });
});
